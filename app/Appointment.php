<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Appointment extends Model
{
    /*public function scopeSearch($query, $s) {
        return $query->where('experience', 'like', '%' .$s. '%')
            ->orWhere('description', 'like', '%' .$s. '%')
            ->orWhere('address', 'like', '%' .$s. '%');
    }

    protected $table = 'advertisements';
    protected $primaryKey = 'advertisement_id';
    public $timestamps = false;
    protected $fillable = [
        'advertisement_id','type','title', 'description', 'bit_end_date', 'image',
        'address', 'lat', 'lng','distance', 'price', 'status', 'user_id', 'category_id','sub_cat_id', 'sub_sub_cat_id','start_date',
        'end_date','created_at','updated_at','zip','experience','user_name',
    ];*/
}
